---
name: forms
description: Vertical exchange forms—labels, validation, decimal inputs, wallet gates.
---

# Forms

Vertical: Label → Input → Helper → Error. One primary submit per section.

Exchange: amount/price/address fields; chain tx shows loading + toast.

Patterns: `factory-create-card.tsx`, `swap-card.tsx`, `create-form` workflow skill.

Rules: `frontend.mdc`, `ui.mdc`.
