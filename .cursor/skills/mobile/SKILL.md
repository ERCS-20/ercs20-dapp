---
name: mobile
description: Mobile layout reorganization—not squeezed desktop grids.
---

# Mobile

Reorganize: single column, bottom actions, Sheets for pickers. No horizontal scroll on primary flows.

Trading: header → chart OR book (tabs) → order form → orders/balances.

Tap targets ≥44px. Reference `ercs20-token-select-sheet.tsx`, `app-header` mobile settings Sheet.

See `trading-page`, `responsive`.
