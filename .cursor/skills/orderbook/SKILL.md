---
name: orderbook
description: Market depth UI—compact rows, spread, in-place updates.
---

# Order Book

Dense table: tabular prices/sizes, sticky spread header, subtle depth bars.

Click price → fill order form. Throttle renders; no flash full reload.

Color + column labels for bid/ask (`accessibility`).

Mobile: split tabs Bids | Asks if 4-column unreadable.

See `tables`, `trading-page`.
