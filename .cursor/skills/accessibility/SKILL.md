---
name: accessibility
description: Keyboard, focus, ARIA, contrast, non-color cues for exchange UI.
---

# Accessibility

Interactive: keyboard + visible focus + `aria-label` when no text.

Never color-only buy/sell; errors need text + `role="alert"`.

Tables: sensible headers; avoid noisy live regions on order book.

Align with `frontend.mdc` a11y bullets.
