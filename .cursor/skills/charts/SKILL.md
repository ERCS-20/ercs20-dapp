---
name: charts
description: Trading chart placement, theming, and render performance.
---

# Charts

Priority pane on trading views. Neutral grid; semantic candle colors (`colors` skill).

Lazy-load chart lib; throttle live updates; isolate from unrelated React state.

Loading: skeleton with fixed aspect ratio.

See `trading-page`, `animation`.
