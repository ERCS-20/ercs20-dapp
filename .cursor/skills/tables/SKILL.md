---
name: tables
description: Dense exchange tables—sticky header, alignment, skeleton, pagination.
---

# Tables

Always: sticky header, hover row, compact density, right-align numbers, left-align text, tabular nums, empty/loading/skeleton, pagination or virtual scroll.

shadcn `Table` in feature folders — no `components/ui` duplicates.

See `orderbook` for depth-specific layout.
