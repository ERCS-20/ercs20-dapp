---
description: 新增依赖时列出包与命令，由用户本机手动安装
alwaysApply: true
---

# npm 依赖安装（用户手动执行）

- 当任务需要 **新增或升级 npm 包** 时：**在回复里明确列出**包名（及版本范围若已写入 `package.json`），并给出一条可复制命令，例如：  
  `npm install <package>[@<version>]` 或 `npm install`（在已改好 `package.json` / lock 之后同步依赖）。
- **不要**假定助手已在当前环境成功执行过 `npm install`；**默认由用户在项目根目录自行运行**上述命令。
- 若只需同步现有 `package.json`，说明执行：`npm install`（或团队若用 lock 对齐则 `npm ci`）。
