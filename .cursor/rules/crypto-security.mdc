---
description: 加密货币相关代码的安全与依赖约束
alwaysApply: true
---

# 加密货币项目安全（依赖与集成）

- **依赖来源**：只使用 **npm 官方 registry** 等可信源；新增包前核对维护方、下载量、仓库与审计记录，**拒绝**来路不明、无人维护或复制粘贴教程里的「小众钱包 SDK」。
- **优先生态**：钱包与链交互优先 **viem / wagmi** 及 **RainbowKit** 等 **社区广泛采用、文档清晰** 的栈；避免为省事引入未经验证的「一键连接」封装。
- **禁止模式**：不要建议用户从**随机网站下载可执行文件**、运行**不明 curl | bash**、或安装**非商店/非官方渠道**的浏览器扩展；配置类需求用**官方控制台生成的 ID/密钥**（如 WalletConnect/Reown Project ID）并说明用途即可。
- **变更习惯**：涉及私钥、签名、合约调用的改动保持**最小 diff**；新增脚本或 CI 步骤需可审查，避免隐蔽外联。
- **若需求模糊**：宁可要求用户明确「官方文档链接」或「要对接的具体协议」，也不要猜测并接入未知第三方服务。
