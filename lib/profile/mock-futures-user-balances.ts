import type { UserBalanceRsp } from "@/services/asset/types";

/** Phase 2 mock — replace with futures balances API when wired. */
export function getMockFuturesUserBalances(): UserBalanceRsp[] {
  return [];
}
