import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /** Allow HMR / dev assets when opening the app from another device on your LAN. Add your machine’s IP if it differs. */
};

export default nextConfig;
